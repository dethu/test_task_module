def generate_expression(left_operand, right_operand, operation):
    expression = {"left_operand": left_operand,
                  "right_operand": right_operand,
                  "operation": operation}
    return expression
